#include <stdio.h>

int main(void)
{
	char letter;

	for (letter = "a"; letter <= "z"; leter++)
	{
		putchar(letter);
	}

	for (letter = "A"; letter <= "Z"; letter++)
	{
		putchar(letter);
	}

	putchar("\n");

	return 0;
}
